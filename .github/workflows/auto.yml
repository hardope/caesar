name: restart the server on every commit

on:
  push:
    branches:
      - main  # Replace with the branch you want to trigger the workflow for

jobs:
  run-script:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Run My Script
        run: |
          # Add commands here to run your script
          ./home/ubuntu/caesar/update
